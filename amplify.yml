version: 1
frontend:
  phases:
    build:
      commands:
        - echo -e "env = { 'APIendpoint1':'$APIendpoing1',\n'APIendpoint2':'$APIendpoing2' };" > env.js
        - cat env.js
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []