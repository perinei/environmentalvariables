version: 1
frontend:
  phases:
    # IMPORTANT - Please verify your build commands
    build:
      commands:
        - echo "inicio"
        # - echo "var vUSER = '$USER';" > env.js
        - printf "env = {\"vUSER\": $USER} " >> env.js
        - ls -la
        - cat env.js
        # - echo "env = {\"USER\": $USER}" >> env.js
        - echo "fim"
  artifacts:
    # IMPORTANT - Please verify your build output directory
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []